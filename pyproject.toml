[project]
name = "tracker-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dbt-duckdb>=1.10.1",
    "dlt>=1.22.1",
    "requests>=2.32.5",
]
